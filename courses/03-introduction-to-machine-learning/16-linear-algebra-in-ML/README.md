https://miro.com/app/board/uXjVHuyI1Ps=/ 

Board 1 — "Spot-the-Bug Gotchas Wall" (for the Stage 4 breakout): four color-coded columns, one per bug, each labeled with its topic and whether it crashes or fails silently. There's a 20-minute countdown, a breakout-rooms launcher, and a seed example sticky in each column showing rooms the format for their own "what was wrong + why" notes.

Board 2 — "KNN — Which K Do You Trust?" (for the Stage 3 playground activity): the three K panels (1/3/7) with predict-then-train prompts, a live poll for the "which K do you trust?" vote, a discussion panel with the K=1 trap and its answer, and a drop zone where rooms paste their K=1 vs K=7 screenshots.

Both use the same color meaning as your Excalidraw boards (red = overfit/trap, green = generalizes/correct, purple = takeaway), so students read them consistently.

Now the Microsoft Forms exit ticket for Stage 5. Since I can't create it directly in your Forms account, here's the complete question set ready to build — I'll give it to you in copy-paste form with the exact question types to select.

Microsoft Forms — Stage 5 Exit Ticket

Form title: Exit Ticket — Feature Engineering & Linear Algebra
Description: Two minutes. Your answers tell me what landed and what to revisit next session.

Q1 — the delta question, Topic 5 (Choice, required)
Question: A colleague fits a StandardScaler on the whole dataset, then splits into train/test. What's the problem?

Nothing, that's correct
Data leakage — test statistics leak into training ✓
The scaler runs too slowly
One-hot encoding will now fail
(This re-asks your Kahoot leakage question — compare the % correct now vs then to measure learning gain.)

Q2 — the delta question, Topic 6 (Choice, required)
Question: In a matrix polynomial, what does A² mean?

A * A, multiply each entry by itself
A @ A, matrix multiplication ✓
The transpose of A
Each entry squared
(Re-asks the @ vs * question — same before/after comparison.)

Q3 — What did I learn? (Long text, required)
In one sentence: what's the most important thing you learned today?

Q4 — What did I understand? (Long text, optional)
What clicked for you — a concept that finally made sense?

Q5 — What will I do next? (Long text, optional)
What will you try or practice before the next session? (e.g. the AutoCode exercises)

Q6 — What's still unclear? (Long text, required)
What's still fuzzy? This is what I'll revisit at the start of next session.

Q7 — Confidence check (Rating, 1–5 stars, required)
How confident do you feel building and understanding a feature matrix now? (1 = lost, 5 = solid)